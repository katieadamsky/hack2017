<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>SMITTY RETURNS</title>
  <script type="text/javascript" src="../../assets/javascripts/chart.js"></script>
</head>
<body>
  <form method="post" action="/pages" style="width: 100%; margin: auto;">
    <fieldset>
      <!-- Form Name -->
      <legend>Welcome to TwitterVision!</legend>

      <!-- Text input-->
      <div class="form-group">
        <label class="control-label" for="textinput">Enter a Twitter Handle</label>
        <div>
          <input id="username" name="username" type="text" placeholder="realDonaldTrump" class="form-control input-md" style="width: 15em;">
        </div>
      </div>

      <!-- Button -->
      <div class="form-group">
        <label class="control-label" for="singlebutton">Analyze this Account</label>
        <div>
            <input value="Enter" type="submit" />
        </div>
      </div>
    </fieldset>
  </form>
  <canvas id="doughnut-chart" height="100"></canvas>
  <script>
    var tweets = []
    <% if !(tweets.nil?) %>
      tweets = <%=raw tweets%>
    <% end %>

    var vPos = 0
    var pos = 0
    var neutral = 0
    var neg = 0
    var vNeg = 0

    for (var i=0; i<tweets.length; i++) {
      if (tweets[i] == 0) continue;
      if (tweets[i] >= 0.8) vPos++;
      else if (tweets[i] >= 0.3) pos++;
      else if (tweets[i] > -0.3) neutral++;
      else if (tweets[i] > -0.8) neg++;
      else vNeg++;
    }

    new Chart(document.getElementById("doughnut-chart"), {
    type: 'doughnut',
    data: {
      labels: ["Very positive", "Positive", "Neutral", "Negative", "Very negative"],
      datasets: [
        {
          label: "Population (millions)",
          backgroundColor: ["#58aceb", "#3e77a3","#2c5575","#1b3549","#000000"],
          data: [vPos,pos,neutral,neg,vNeg]
        }
      ]
    },
    options: {
      title: {
        display: true,
        text: 'Sentiment Analysis'
      }
    }
    });

  </script>
</body>
</html>
